# Nuclear Throne - Splitux Handler
# GameMaker + goldberg. Local-split couch co-op over splitux-together.
#
# VALIDATED 2026-06-17: boots + streams clean (60fps internal cap); local-split couch via together.

name: Nuclear Throne
exec: "nuclearthrone.exe"
author: splitux
version: "1.0"
info: |
  Nuclear Throne local-split couch co-op over splitux-together: two together seats
  fold into a single game instance (collapse_for_local_split). GameMaker + goldberg;
  goldberg only satisfies the Steam API init, local couch co-op needs no networking.
spec_ver: 3
steam_appid: 242680

coop_mode: local-split
input_isolation: none
goldberg:
  disable_networking: true
