# Pizza Possum - Splitux Handler
# Unity + goldberg. Local-split couch co-op over splitux-together.
#
# VALIDATED 2026-06-17: launches + streams (vulkan-zerocopy); local-split couch via together.

name: Pizza Possum
exec: "PizzaPossum.exe"
author: splitux
version: "1.0"
info: |
  Pizza Possum local-split couch co-op over splitux-together: two together seats
  fold into a single game instance (collapse_for_local_split). Unity + goldberg;
  goldberg only satisfies the Steam API init, local couch co-op needs no networking.
spec_ver: 3
steam_appid: 1951230

coop_mode: local-split
input_isolation: none
goldberg:
  disable_networking: true
